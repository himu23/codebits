#include <bits/stdc++.h>
using namespace std;
#define ll long long int

int main() {
    cin.tie(0)->sync_with_stdio(false);
    int n;
    while(cin>>n){
        if(n==42) break;
        cout<<n<<endl;
    }
    return 0;
}